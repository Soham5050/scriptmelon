# ScriptMelon Python requirements
#
# Install from the project root:
#   python -m pip install -U pip setuptools wheel
#   python -m pip install -r requirements.txt
#   python -m pip install --no-deps git+https://github.com/ai4bharat/IndicF5.git
#
# That last line is a separate step because pip requirements files cannot carry
# --no-deps, and IndicF5 must be installed without it. See the IndicF5 block below.
#
# System dependency:
#   FFmpeg must be installed and available on PATH.
#
# GPU note:
#   These PyTorch pins target the CUDA 12.8 build used by the RTX 50-series
#   development setup. For CPU-only or another CUDA version, replace the three
#   torch package lines with the matching install command from pytorch.org.

--extra-index-url https://download.pytorch.org/whl/cu128

# Core model runtime
torch==2.8.0+cu128
torchaudio==2.8.0+cu128
torchvision==0.23.0+cu128
transformers==4.57.3
accelerate==1.12.0
bitsandbytes>=0.49.2
huggingface-hub>=0.36.2
safetensors>=0.7.0
sentencepiece>=0.2.0
sacremoses>=0.1.1
protobuf>=3.20.0

# Local Qwen3-TTS package
-e ./Qwen3-TTS

# IndicF5 (Indic-language TTS) -- installed separately, on purpose:
#
#   python -m pip install --no-deps git+https://github.com/ai4bharat/IndicF5.git
#
# The --no-deps is required, not a shortcut. IndicF5's own requirements.txt
# declares numpy<=1.26.4 and transformers<4.50; both pins are stale. A plain
# install silently downgrades numpy 2.x -> 1.26.4 and transformers 4.57 -> 4.49,
# which breaks Qwen3-TTS (pinned transformers==4.57.3 above), torch 2.8, whisperx
# and demucs. Verified: f5_tts imports and runs fine against numpy 2.3.5 /
# transformers 4.57.3, so the pins are the problem, not the code.
#
# Because --no-deps installs nothing, f5_tts's real import closure is pinned here
# instead. Probed by importing f5_tts.infer.utils_infer and f5_tts.model and
# reading sys.modules, so this is what it actually needs, not what it declares.
# wandb looks out of place but f5_tts/model/trainer.py imports it at module level,
# so `import f5_tts.model` fails without it.
jieba>=0.42.1
vocos>=0.1.0
x-transformers>=1.31.14
torchdiffeq>=0.2.5
ema-pytorch>=0.5.2
pypinyin>=0.53
pydub>=0.25.1
matplotlib>=3.8
click>=8.1
wandb>=0.17
# Not in that closure, but f5_tts/api.py needs it and the IndicF5 HF repo's
# trust_remote_code module is gated, so we cannot confirm it stays clear of api.py.
cached-path>=1.6

# Audio, DSP, and source separation
numpy>=2.0,<3
scipy>=1.11
soundfile>=0.12.1
librosa>=0.11.0
sox>=1.5.0
soxr>=1.0.0
onnxruntime>=1.20.0
einops>=0.8.0
demucs>=4.0.1

# ASR
faster-whisper>=1.2.0
whisperx>=3.8.1

# Translation and APIs
deep-translator>=1.11.4
openai>=1.30.0
requests>=2.31.0

# Desktop studio GUI
dearpygui>=2.2

# Config and utilities
python-dotenv>=1.0.0
tqdm>=4.67.0
rich>=13.7.0

# Training / dataset preparation utilities
datasets>=4.5.0
scikit-learn>=1.4.0
pandas>=2.2.0

# Optional quality/research extras:
# sacrebleu>=2.4.0
# sentence-transformers>=2.5.0
